import { cn } from '../../utils';

const Dashboard: React.FC = () => {
    return (
        <>
            <h1 className={cn('flex items-center justify-center h-screen w-screen')}>Dashboard</h1>
        </>
    )
}

export default Dashboard;